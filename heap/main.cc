#include <iostream>
#include <vector>

using namespace std;

class Heap {
    vector<int> heap;
    Heap(int x) {
        heap.push_back(x);
    }
    insert(int x) {
        
    }
}

int main() {
    
}
